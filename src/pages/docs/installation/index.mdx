import DocsLayout from "../../components/docs/layout.tsx";

# Installation

This section explains how to install My Company products.

export default function MDXPage({ children }) {
  return <DocsLayout>{children}</DocsLayout>;
}
